<div class="min-h-screen flex flex-col items-center justify-center bg-gray-50 py-6 px-4 sm:px-6 lg:px-8">
  <div id="prompt-section" class="text-center max-w-4xl mx-auto">
    <img src="https://service-jobs-images.s3.us-east-2.amazonaws.com/7rl98t1weu387r43il97h6ipk1l7" alt="LlamaPress Logo" class="mx-auto w-16 h-16 sm:w-24 sm:h-24 mb-3 sm:mb-4">
    <h1 class="text-5xl sm:text-6xl font-bold text-gray-800 mb-4">Build On Brand Pages Quickly with AI</h1>
    <p class="text-xl text-gray-600 mb-8">Enter your site URL for us to scan your brand guidelines</p>
    
    <%# <div class="bg-white p-6 rounded-lg shadow-md max-w-2xl mx-auto">
      <textarea id="website-prompt" class="w-full h-32 p-4 border border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500" placeholder=""></textarea>
      <button onclick="showSignupForm()" class="mt-4 w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
        Scan My Site for Brand Guidelines
      </button>
    </div> %>
  </div>

  <div id="signup-section" class="w-full max-w-md">
    <div class="bg-white p-6 sm:p-8 rounded-lg shadow-md">
      <%# <div class="text-center mb-6 sm:mb-8">
        <img src="https://service-jobs-images.s3.us-east-2.amazonaws.com/7rl98t1weu387r43il97h6ipk1l7" alt="LlamaPress Logo" class="mx-auto w-16 h-16 sm:w-24 sm:h-24 mb-3 sm:mb-4">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-800">Sign Up</h2>
      </div> %>

      <form action="/public_leads/register_and_brand_clone" method="POST" class="space-y-4 sm:space-y-6">
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <input type="hidden" name="prompt" id="hidden-prompt" required>

        <div>
          <label for="website_url" class="block text-sm font-medium text-gray-700">URL to scan *</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-globe text-gray-400"></i>
            </div>
            <input type="url" name="website_url" id="website_url" required class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="https://examplesite.com">
          </div>
        </div>

        <div>
          <label for="business_name" class="block text-sm font-medium text-gray-700">Business Name</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-building text-gray-400"></i>
            </div>
            <input type="text" name="business_name" id="business_name" required class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="Your Business Name">
          </div>
        </div>

        <div>
          <label for="full_name" class="block text-sm font-medium text-gray-700">Your Name</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-user text-gray-400"></i>
            </div>
            <input type="text" name="full_name" id="full_name" required class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="Full Name">
          </div>
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-envelope text-gray-400"></i>
            </div>
            <input type="email" name="email" id="email" required autocomplete="email" class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="you@example.com">
          </div>
        </div>

        <div>
          <label for="phone" class="block text-sm font-medium text-gray-700">Phone Number</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-phone text-gray-400"></i>
            </div>
            <input type="tel" name="phone" id="phone" required class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="(555) 555-5555">
          </div>
        </div>

        <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Scan My Site
        </button>
      </form>

      <%# <div class="mt-4 sm:mt-6 text-center text-sm text-gray-600">
        Already have an account? 
        <a href="#" onclick="showSigninForm()" class="font-medium text-indigo-600 hover:text-indigo-500">Sign in</a>
      </div> %>
    </div>
  </div>

  <div id="signin-section" class="hidden w-full max-w-md">
    <div class="bg-white p-6 sm:p-8 rounded-lg shadow-md">
      <div class="text-center mb-6 sm:mb-8">
        <img src="https://service-jobs-images.s3.us-east-2.amazonaws.com/7rl98t1weu387r43il97h6ipk1l7" alt="LlamaPress Logo" class="mx-auto w-16 h-16 sm:w-24 sm:h-24 mb-3 sm:mb-4">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-800">Sign In</h2>
      </div>

      <form action="/public_leads/create_from_prompt" method="POST" class="space-y-4 sm:space-y-6">
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <input type="hidden" name="prompt" id="hidden-prompt-signin">

        <div>
          <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-envelope text-gray-400"></i>
            </div>
            <input type="email" name="email" id="email-signin" required autocomplete="email" class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="you@example.com">
          </div>
        </div>

        <div>
          <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
          <div class="mt-1 relative rounded-md shadow-sm">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-lock text-gray-400"></i>
            </div>
            <input type="password" name="password" id="password-signin" required autocomplete="current-password" class="pl-10 block w-full rounded-md border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-sm" placeholder="••••••••">
          </div>
        </div>

        <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Sign in and create website
        </button>
      </form>

      <div class="mt-4 sm:mt-6 text-center text-sm text-gray-600">
        Don't have an account? 
        <a href="#" onclick="showSignupForm()" class="font-medium text-indigo-600 hover:text-indigo-500">Sign up</a>
      </div>
    </div>
  </div>
</div>

<script>
const placeholders = [
  "Create a team page that automatically updates when I add new staff...",
  "Build me a product showcase with filters and sorting options...",
  "I need a contact form that sends quotes based on user selections...",
  "Make a portfolio that organizes projects by category...",
  "Design a services page with interactive pricing calculator..."
];

let currentPlaceholderIndex = 0;
let currentCharIndex = 0;
let isDeleting = false;
let typingSpeed = 50;
let deletingSpeed = 30;
let pauseBeforeDelete = 2000;
let pauseBeforeNextPhrase = 500;

function typeEffect() {
  const textarea = document.getElementById('website-prompt');
  const currentPlaceholder = placeholders[currentPlaceholderIndex];

  if (isDeleting) {
    // Deleting text
    currentCharIndex--;
    textarea.setAttribute('placeholder', currentPlaceholder.substring(0, currentCharIndex));
    
    if (currentCharIndex === 0) {
      isDeleting = false;
      currentPlaceholderIndex = (currentPlaceholderIndex + 1) % placeholders.length;
      setTimeout(typeEffect, pauseBeforeNextPhrase);
      return;
    }
    setTimeout(typeEffect, deletingSpeed);
  } else {
    // Typing text
    currentCharIndex++;
    textarea.setAttribute('placeholder', currentPlaceholder.substring(0, currentCharIndex));
    
    if (currentCharIndex === currentPlaceholder.length) {
      isDeleting = true;
      setTimeout(typeEffect, pauseBeforeDelete);
      return;
    }
    setTimeout(typeEffect, typingSpeed);
  }
}

// Start the typing effect when the page loads
document.addEventListener('DOMContentLoaded', function() {
  typeEffect();
  // Get the prompt from localStorage and set it in the hidden field
  const prompt = localStorage.getItem('website_prompt');
  if (prompt) {
    document.getElementById('hidden-prompt').value = prompt;
    document.getElementById('website-prompt').value = prompt;
  }
});

function showSigninForm() {
  const prompt = document.getElementById('website-prompt').value;
  if (prompt.trim() === '') {
    alert('Please enter your website idea first');
    return;
  }
  
  // Set the prompt in the hidden field
  document.getElementById('hidden-prompt-signin').value = prompt;
  
  // Hide prompt section and show signin form
  document.getElementById('prompt-section').classList.add('hidden');
  document.getElementById('signup-section').classList.add('hidden');
  document.getElementById('signin-section').classList.remove('hidden');
}

function showSignupForm() {
  const prompt = document.getElementById('website-prompt').value;
  if (prompt.trim() === '') {
    alert('Please enter your website idea first');
    return;
  }
  
  // Set the prompt in the hidden field
  document.getElementById('hidden-prompt').value = prompt;
  
  // Hide prompt section and show signup form
  document.getElementById('prompt-section').classList.add('hidden');
  document.getElementById('signin-section').classList.add('hidden');
  document.getElementById('signup-section').classList.remove('hidden');
}
</script>
