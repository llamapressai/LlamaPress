<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You!</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-r from-blue-100 to-purple-100">
    <div class="flex items-center justify-center min-h-screen">
        <div class="max-w-md w-full bg-white shadow-2xl rounded-lg p-10 text-center">
            <i class="fas fa-check-circle text-6xl text-green-500 mb-6"></i>
            <h1 class="text-4xl font-bold text-gray-800 mb-4">Thank You!</h1>
            
            <p class="text-xl text-gray-600 mb-8">Your submission has been received successfully.</p>
            
            <div class="flex justify-center space-x-4 mb-8">
                <i class="fas fa-envelope text-3xl text-blue-500"></i>
                <i class="fas fa-file-alt text-3xl text-blue-500"></i>
                <i class="fas fa-thumbs-up text-3xl text-blue-500"></i>
            </div>
            
            <div class="text-left mt-8">
                <h2 class="text-2xl font-semibold text-gray-800 mb-4">Details:</h2>
                <% if @submission.present? %>
                    <% @submission.data.except('page_id').each do |key, value| %>
                        <div class="mb-2">
                            <span class="font-medium text-gray-700"><%= key.to_s.titleize %>:</span>
                            <span class="text-gray-600"><%= value %></span>
                        </div>
                    <% end %>
                <% else %>
                    <p class="text-gray-600">No submission details available.</p>
                <% end %>
            </div>
        </div>
    </div>
</body>
</html>